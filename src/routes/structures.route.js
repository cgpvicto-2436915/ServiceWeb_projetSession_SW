import { trouverStructure } from "../controllers/structures.controller.js";
import express from 'express';

const router = express.Router();
//router.get('/:id', trouverStructure);



export default router;